import 'package:flutter/material.dart';
import 'package:waldo/core/theme/spacing.dart';

class Rounded {
  Rounded._();

  static final smRadius = BorderRadius.circular(Spacing.sm);
  static final mdRadius = BorderRadius.circular(Spacing.md);
  static final lgRadius = BorderRadius.circular(Spacing.lg);
  static final xlRadius = BorderRadius.circular(Spacing.xl);

  static final pillRadius = BorderRadius.circular(9999);
}
